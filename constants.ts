// Swipe gesture thresholds - adjusted for better mobile responsiveness
export const SWIPE_UP_THRESHOLD = -60; // Easier to expand (was -100)
export const SWIPE_DOWN_THRESHOLD = 80; // Easier to collapse/close (was 150)

// Animation durations (ms)
export const MODAL_TRANSITION_DURATION = 350; // Slightly faster (was 450)
export const DRAG_RESET_DELAY = 10;
export const CLOSE_ANIMATION_DURATION = 300; // Slightly faster (was 400)

// Responsive breakpoints
export const MOBILE_BREAKPOINT = 640;
